import 'package:flutter/material.dart';

class FarmerAwareness extends StatelessWidget {
  const FarmerAwareness({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Farmer Awareness"),),
    );  }
}